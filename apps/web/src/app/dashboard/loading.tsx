import { DashboardSkeleton } from '@/components/dashboard'

/**
 * Dashboard Page Loading State
 *
 * Shows skeleton UI while the dashboard page is loading.
 */
export default function DashboardLoading() {
  return <DashboardSkeleton />
}
