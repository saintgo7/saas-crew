# Plan 08-02: 배포 환경 구축 (학교 서버)

---
phase: 8
plan_number: 02
goal: Docker 기반 프로덕션 배포 환경 완성
depends_on: None
parallelizable: true
estimated_tasks: 5
files_modified:
  - docker-compose.prod.yml
  - apps/api/.env.production.example
  - apps/web/.env.production.example
  - deployments/scripts/*.sh
  - docs/DEPLOYMENT_GUIDE.md
---

## Context

**Current State:**
- Dockerfiles 완성 (multi-stage builds)
- docker-compose.yml 개발 환경용
- docker-compose.prod.yml 존재하지만 미완성
- 학교 서버 배포 스크립트 부재

**Target Environment:**
- 학교 서버: Docker + PostgreSQL
- Frontend: Cloudflare Pages (무료)
- 데이터베이스: PostgreSQL 16
- 캐싱: Redis 7

**Goal:**
- docker-compose.prod.yml 완성
- 배포 스크립트 자동화
- 환경 변수 관리 체계화
- 데이터베이스 마이그레이션 자동화
- 학교 서버 배포 가이드 작성

**Success Criteria:**
- [ ] docker-compose.prod.yml 프로덕션 ready
- [ ] 배포 스크립트 5개 작성 (deploy, rollback, backup, logs, health)
- [ ] 환경 변수 .example 파일 작성
- [ ] Prisma 마이그레이션 자동화
- [ ] DEPLOYMENT_GUIDE.md 작성

## Tasks

### Task 1: docker-compose.prod.yml 완성

**Objective:** 프로덕션 환경 docker-compose 설정 완성

**Steps:**
1. Read existing docker-compose.prod.yml
2. Add/update services:
   - api (NestJS backend)
   - postgres (PostgreSQL 16 with persistence)
   - redis (caching layer)
   - nginx (reverse proxy)
3. Configure volumes for data persistence
4. Set up networks for service isolation
5. Add health checks for all services
6. Configure restart policies

**Code to Add (docker-compose.prod.yml):**
```yaml
version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: wku-crew-postgres
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./deployments/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
    ports:
      - "5432:5432"
    networks:
      - wku-crew-network
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: wku-crew-redis
    command: redis-server --requirepass ${REDIS_PASSWORD} --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - wku-crew-network
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # API Backend
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      args:
        NODE_ENV: production
    container_name: wku-crew-api
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      JWT_SECRET: ${JWT_SECRET}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GITHUB_CALLBACK_URL: ${GITHUB_CALLBACK_URL}
      FRONTEND_URL: ${FRONTEND_URL}
      PORT: 4000
    ports:
      - "4000:4000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - wku-crew-network
    restart: always
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: wku-crew-nginx
    volumes:
      - ./deployments/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deployments/ssl:/etc/nginx/ssl:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - api
    networks:
      - wku-crew-network
    restart: always
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 3s
      retries: 3

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  wku-crew-network:
    driver: bridge
```

**Files to Create/Update:**
- docker-compose.prod.yml (update)
- deployments/nginx.conf (new)
- deployments/init-db.sh (new)

**Expected Output:**
- 완전한 프로덕션 docker-compose 설정
- 서비스 간 의존성 및 health check 설정
- 데이터 영속성 보장

---

### Task 2: 환경 변수 관리 체계화

**Objective:** 프로덕션 환경 변수 .example 파일 작성

**Steps:**
1. Create apps/api/.env.production.example
2. Create apps/web/.env.production.example
3. Create deployments/.env.example (docker-compose용)
4. Document all environment variables with descriptions
5. Add security notes for sensitive variables

**Code to Add (.env.production.example):**
```bash
# =============================================================================
# WKU Software Crew - Production Environment Variables
# Copy this file to .env.production and fill in actual values
# NEVER commit .env.production to git
# =============================================================================

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=wku_crew_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=wku_crew_production
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD
REDIS_URL=redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}

# Application Configuration
NODE_ENV=production
PORT=4000
API_URL=https://api.wku-crew.com
FRONTEND_URL=https://wku-crew.com

# JWT Authentication
JWT_SECRET=CHANGE_ME_LONG_RANDOM_STRING_AT_LEAST_32_CHARS
JWT_EXPIRES_IN=7d

# GitHub OAuth
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_CALLBACK_URL=https://api.wku-crew.com/api/auth/github/callback

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# Logging
LOG_LEVEL=info
LOG_FILE_PATH=/var/log/wku-crew/api.log

# Monitoring (Optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production

# Email (Optional for notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@wku-crew.com
```

**Files to Create:**
- apps/api/.env.production.example (new)
- apps/web/.env.production.example (new)
- deployments/.env.example (new)
- docs/ENVIRONMENT_VARIABLES.md (new)

**Expected Output:**
- 완전한 환경 변수 템플릿
- 보안 가이드 포함
- 학교 서버 관리자용 문서

---

### Task 3: 배포 스크립트 자동화

**Objective:** 배포, 롤백, 백업 등 운영 스크립트 작성

**Steps:**
1. Create deployments/scripts/ directory
2. Create deploy.sh (초기 배포)
3. Create update.sh (업데이트 배포)
4. Create rollback.sh (이전 버전 복구)
5. Create backup.sh (데이터베이스 백업)
6. Create logs.sh (로그 조회)
7. Create health.sh (헬스 체크)
8. Make all scripts executable (chmod +x)

**Code to Add (deploy.sh):**
```bash
#!/bin/bash
# =============================================================================
# WKU Software Crew - Initial Deployment Script
# =============================================================================

set -e

echo "🚀 Starting WKU Software Crew deployment..."

# Load environment variables
if [ -f .env.production ]; then
    export $(cat .env.production | grep -v '#' | xargs)
else
    echo "❌ Error: .env.production not found"
    exit 1
fi

# Pull latest images
echo "📦 Pulling latest Docker images..."
docker-compose -f docker-compose.prod.yml pull

# Build API image
echo "🔨 Building API Docker image..."
docker-compose -f docker-compose.prod.yml build api

# Start services
echo "🏃 Starting services..."
docker-compose -f docker-compose.prod.yml up -d

# Wait for database to be ready
echo "⏳ Waiting for database..."
sleep 10

# Run database migrations
echo "🗄️  Running database migrations..."
docker-compose -f docker-compose.prod.yml exec -T api pnpm db:migrate:deploy

# Verify deployment
echo "🔍 Verifying deployment..."
for i in {1..5}; do
    if curl -sf http://localhost:4000/api/health > /dev/null; then
        echo "✅ Deployment successful! API is healthy."
        exit 0
    fi
    echo "Attempt $i failed, retrying..."
    sleep 5
done

echo "❌ Deployment verification failed"
exit 1
```

**Code to Add (backup.sh):**
```bash
#!/bin/bash
# =============================================================================
# Database Backup Script
# =============================================================================

set -e

BACKUP_DIR="./backups"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/wku_crew_backup_$TIMESTAMP.sql.gz"

mkdir -p $BACKUP_DIR

echo "🗄️  Starting database backup..."

docker-compose -f docker-compose.prod.yml exec -T postgres \
    pg_dump -U $DB_USER $DB_NAME | gzip > $BACKUP_FILE

echo "✅ Backup completed: $BACKUP_FILE"

# Keep only last 7 backups
ls -t $BACKUP_DIR/*.sql.gz | tail -n +8 | xargs -r rm

echo "🧹 Old backups cleaned up"
```

**Files to Create:**
- deployments/scripts/deploy.sh (new)
- deployments/scripts/update.sh (new)
- deployments/scripts/rollback.sh (new)
- deployments/scripts/backup.sh (new)
- deployments/scripts/logs.sh (new)
- deployments/scripts/health.sh (new)

**Expected Output:**
- 6개 운영 스크립트 생성
- 배포 자동화 완성
- 백업/복구 메커니즘 구현

---

### Task 4: Nginx 리버스 프록시 설정

**Objective:** Nginx 설정으로 API 프록시 및 SSL 준비

**Steps:**
1. Create deployments/nginx.conf
2. Configure reverse proxy for API
3. Add CORS headers
4. Add rate limiting (nginx level)
5. Add SSL configuration (placeholder)
6. Add gzip compression
7. Add security headers

**Code to Add (nginx.conf):**
```nginx
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript
               application/json application/javascript application/xml+rss;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_req_status 429;

    # Upstream API
    upstream api_backend {
        server api:4000;
    }

    # HTTP Server
    server {
        listen 80;
        server_name api.wku-crew.com;

        # Health check endpoint
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }

        # API Proxy
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;

            proxy_pass http://api_backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;

            # CORS headers (if frontend different domain)
            add_header Access-Control-Allow-Origin $http_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Authorization, Content-Type" always;
            add_header Access-Control-Allow-Credentials "true" always;

            if ($request_method = 'OPTIONS') {
                return 204;
            }

            # Security headers
            add_header X-Frame-Options "DENY" always;
            add_header X-Content-Type-Options "nosniff" always;
            add_header X-XSS-Protection "1; mode=block" always;
            add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        }

        # Redirect HTTP to HTTPS (production)
        # Uncomment when SSL is set up
        # return 301 https://$server_name$request_uri;
    }

    # HTTPS Server (placeholder for SSL)
    # server {
    #     listen 443 ssl http2;
    #     server_name api.wku-crew.com;
    #
    #     ssl_certificate /etc/nginx/ssl/cert.pem;
    #     ssl_certificate_key /etc/nginx/ssl/key.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #
    #     location / {
    #         proxy_pass http://api_backend;
    #         # ... same proxy settings as above ...
    #     }
    # }
}
```

**Files to Create:**
- deployments/nginx.conf (new)
- deployments/ssl/.gitkeep (new, for SSL certs)

**Expected Output:**
- Nginx 리버스 프록시 설정
- Rate limiting (nginx level)
- SSL 준비 완료 (placeholder)

---

### Task 5: 배포 가이드 문서 작성

**Objective:** DEPLOYMENT_GUIDE.md 작성 (학교 서버 관리자용)

**Steps:**
1. Create docs/DEPLOYMENT_GUIDE.md
2. Document server requirements
3. Step-by-step deployment instructions
4. Troubleshooting guide
5. Maintenance procedures
6. Monitoring setup

**Code to Add (DEPLOYMENT_GUIDE.md):**
```markdown
# WKU Software Crew - Deployment Guide

## Server Requirements

### Hardware
- CPU: 2 cores minimum (4 cores recommended)
- RAM: 4GB minimum (8GB recommended)
- Storage: 20GB minimum (50GB recommended for logs/backups)
- Network: Stable internet connection

### Software
- Ubuntu 22.04 LTS (recommended) or CentOS 8+
- Docker 24.0+
- Docker Compose 2.0+
- Git 2.0+

## Initial Setup

### 1. Install Docker

```bash
# Update system
sudo apt update && sudo apt upgrade -y

# Install Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Add user to docker group
sudo usermod -aG docker $USER
newgrp docker

# Install Docker Compose
sudo apt install docker-compose-plugin -y

# Verify installation
docker --version
docker compose version
```

### 2. Clone Repository

```bash
cd /opt
sudo git clone https://github.com/wku-crew/saas-crew.git
cd saas-crew
```

### 3. Configure Environment

```bash
# Copy environment templates
cp apps/api/.env.production.example apps/api/.env.production
cp apps/web/.env.production.example apps/web/.env.production
cp deployments/.env.example .env.production

# Edit environment files
nano .env.production
# Fill in:
# - Strong DB_PASSWORD
# - Strong REDIS_PASSWORD
# - Strong JWT_SECRET (32+ characters)
# - GitHub OAuth credentials
# - Production URLs
```

### 4. Deploy Application

```bash
# Make scripts executable
chmod +x deployments/scripts/*.sh

# Run initial deployment
./deployments/scripts/deploy.sh
```

## Monitoring

### Check Service Health

```bash
./deployments/scripts/health.sh
```

### View Logs

```bash
# All services
./deployments/scripts/logs.sh

# Specific service
docker-compose -f docker-compose.prod.yml logs -f api
```

### Resource Usage

```bash
docker stats
```

## Backup & Restore

### Create Backup

```bash
./deployments/scripts/backup.sh
```

Backups are stored in `./backups/` directory.

### Restore from Backup

```bash
# Stop services
docker-compose -f docker-compose.prod.yml down

# Restore database
gunzip < backups/wku_crew_backup_TIMESTAMP.sql.gz | \
    docker-compose -f docker-compose.prod.yml exec -T postgres \
    psql -U $DB_USER $DB_NAME

# Restart services
docker-compose -f docker-compose.prod.yml up -d
```

## Updates

### Deploy New Version

```bash
# Pull latest code
git pull origin main

# Run update script
./deployments/scripts/update.sh
```

### Rollback

```bash
# Rollback to previous version
./deployments/scripts/rollback.sh
```

## Troubleshooting

### API Not Responding

1. Check if container is running: `docker ps`
2. Check logs: `docker-compose -f docker-compose.prod.yml logs api`
3. Check health: `curl http://localhost:4000/api/health`
4. Restart: `docker-compose -f docker-compose.prod.yml restart api`

### Database Connection Failed

1. Check if postgres is running: `docker ps | grep postgres`
2. Check database logs: `docker-compose -f docker-compose.prod.yml logs postgres`
3. Verify credentials in .env.production
4. Check network: `docker network ls`

### High Memory Usage

1. Check stats: `docker stats`
2. Increase server RAM or optimize queries
3. Adjust Redis maxmemory in docker-compose.prod.yml

## Security Checklist

- [ ] Change all default passwords
- [ ] Use strong JWT_SECRET (32+ chars)
- [ ] Configure firewall (allow only 80, 443, 22)
- [ ] Set up SSL certificates (Let's Encrypt)
- [ ] Enable automatic security updates
- [ ] Regular backups scheduled (daily)
- [ ] Monitor logs for suspicious activity

## Contact

For deployment support:
- GitHub Issues: https://github.com/wku-crew/saas-crew/issues
- Email: devops@wku-crew.com
```

**Files to Create:**
- docs/DEPLOYMENT_GUIDE.md (new, ~400 lines)

**Expected Output:**
- 완전한 배포 가이드
- 학교 서버 관리자가 단독으로 배포 가능
- 트러블슈팅 가이드 포함

---

## Dependencies

**Before Starting:**
- [ ] Dockerfiles 완성 확인 (Phase 7)
- [ ] PostgreSQL 스키마 완성 (Prisma)
- [ ] GitHub OAuth 앱 등록

**After Completion:**
- docker-compose.prod.yml로 전체 스택 실행 가능
- 배포 스크립트로 자동화 배포 가능
- 백업/복구 메커니즘 작동

---

## Validation Checklist

- [ ] docker-compose.prod.yml 실행 성공
- [ ] PostgreSQL 데이터 영속성 확인
- [ ] Redis 캐싱 작동 확인
- [ ] API health check 통과
- [ ] Nginx 리버스 프록시 작동
- [ ] 배포 스크립트 6개 실행 가능
- [ ] 환경 변수 .example 파일 작성
- [ ] DEPLOYMENT_GUIDE.md 완성
- [ ] 백업/복구 테스트 성공

---

## Success Metrics

| Metric | Target | Measurement |
|--------|--------|-------------|
| Docker Compose 실행 | 성공 | docker-compose up -d |
| 서비스 Health Check | 100% | All services healthy |
| 배포 스크립트 | 6개 작동 | Run all scripts |
| 백업 자동화 | 작동 | backup.sh 실행 |
| 문서 완성도 | 90%+ | Review DEPLOYMENT_GUIDE.md |

---

## Risks and Mitigation

**Risk 1:** 학교 서버 스펙 부족
- **Mitigation:** 최소 요구사항 문서화, 리소스 모니터링 스크립트

**Risk 2:** 네트워크 방화벽 문제
- **Mitigation:** 필요 포트 목록 제공, 학교 IT 부서와 사전 협의

**Risk 3:** SSL 인증서 설정 복잡도
- **Mitigation:** Let's Encrypt 자동화 스크립트 제공 (Plan 08-03)

---

## Next Steps

After completing this plan:
1. 학교 서버에서 테스트 배포
2. SSL 인증서 설정 (Plan 08-03)
3. CI/CD 파이프라인 완성 (Plan 08-03)
4. 모니터링 대시보드 설정 (Plan 08-04)

---

**Plan Owner:** DevOps Team
**Reviewers:** Backend Team, Infrastructure Team
**Estimated Duration:** 6-8 hours
**Priority:** Critical (베타 런칭 필수)
